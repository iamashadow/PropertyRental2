import 'package:flutter/material.dart';

class AboutUsOurStoryMobilePage extends StatelessWidget {
  const AboutUsOurStoryMobilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Test Mobile"),),
    );
  }
}
